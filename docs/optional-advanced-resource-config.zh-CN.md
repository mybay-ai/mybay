# 可选的高级资源配置

麦贝开源版 默认将实例级 CPU、内存与磁盘策略的手动管理从开源版主流程中分离。新实例使用 `.env` 中的宿主机保护默认值，同时继续启用 Docker 资源限制与磁盘保护。

如需在开发环境恢复可选的管理界面和接口，可设置：

```env
MYBAY_ADVANCED_RESOURCE_CONFIG_ENABLED=true
```

关闭时，部署请求不能覆盖默认资源值，资源策略管理接口不可用；编辑已有实例的其他设置时，会保留该实例原有的资源配置。
