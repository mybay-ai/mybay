# Hermes / Pi / Codex recovery acceptance — 2026-09-09

## 范围 / Scope

本轮在独立控制服务 `127.0.0.1:4360` 和专用 Runtime 实例中执行真实验收。正式本地服务 `4348` 与原有 Hermes/Pi 实例未重启。Claude P3 继续暂停；没有发布、推送或提升认证等级。

Pi 使用现有镜像加本地 bridge 源码只读覆盖；控制服务加载本地构建。以下是候选代码的本地证据，不代表打包镜像或生产认证。各次尝试使用了不同候选版本；JSON 中的 sourceHashes 是最终候选快照，不能倒推为所有历史尝试的版本。

## 已修复 / Changes

- Pi 高频文本先合并再发送，滚动事件窗口保留单调递增的 SSE 序号。
- 控制侧将连续文本增量合并为 100ms 批次；终态前刷新，关闭时清理。沿用重连去重及上游 Run 身份检查。
- 失败、取消或过期收尾没有最终文本时，保留已持久化的部分输出；状态仍为失败或中断，工具协议泄漏防护继续生效。

## 真实结果 / Live evidence

| 场景 | Hermes | Pi | Codex |
|---|---|---|---|
| 基础真实对话 | PASS | PASS | PASS |
| 断开一个真实上游 SSE 连接，终态文本与原生结果一致 | PASS | PASS | PASS |
| 输出途中停止，产品与原生状态均取消 | PASS | PASS | PASS |
| Runtime 重启后继续已完成会话，原生会话身份与记忆一致 | PASS | PASS | PASS |
| 运行中途重启：失败收敛、单次派发、保留部分文本 | PASS | PASS | PASS |
| 页面重新加载恢复审批 | NOT_RUN | PASS，拒绝后未写文件 | PASS，允许写文件 |
| 控制服务重启恢复待审批 | NOT_RUN | PASS，允许后写文件 | PASS，拒绝后未写文件 |

Codex 页面审批测试中的后续只读文件校验审批通过 API 完成，未计为第二次浏览器点击成功。Hermes 的简单文件写入直接获准，没有产生待审批请求，因此不能据此证明其审批恢复。

真实断线证据证明断线发生且最终文本一致；不能仅凭最终快照断言浏览器从未短暂显示重复文本。序号去重另有自动测试支持。

三种 Runtime 在首次运行中途重启后均失败收敛、原生 Run ID 不变且仅派发一次，但部分文本变成空字符串。这是实际缺陷，原始记录保留为 FAIL。修复后 Hermes 保留 419 字符、Codex 保留 305 字符；Pi 从重启请求前的 1815 字符增长至 4330 字符后停止并保留。复验见配套 JSON 中带时间戳的 `inflight` 记录。

## 检查与限制 / Validation and limits

- 共享流解析与协调相关 71 项测试通过；Pi 原生 Node 测试 27 项通过。
- 新增中断文本保留与既有收尾测试 12 项通过，包括工具协议不恢复的回归用例。
- 全量 `release:gate` 首次失败：1993 PASS、1 FAIL、6 SKIP。唯一失败是租约仓储用例超过原有 5000ms 限制（观察到 5862ms）；不修改超时，原文件复跑 10/10 PASS。整套门禁未重新获得 PASS。
- 后续独立执行的 46 项 bridge Node 测试、编码、文案、i18n、API 错误合同及严格认证校验通过。其中 Claude 仅运行现有候选测试，未继续开发或真实接入。
- 最终构建、TypeScript 检查及新增收尾代码 ESLint 通过；日志与原始辅助脚本留在本地忽略目录 `tmp/runtime-expansion`。共享的脱敏证据保存在本报告旁的 `recovery-acceptance-20260909.json` 中。

首次 Pi 高频输出和随后 Hermes 高频输出曾导致候选控制服务无响应，分别推动 bridge 与共享控制侧批处理修复。错误端口、恢复游标、限流与未观察到停止窗口等失败尝试也保留，未删除后只展示成功。

下一步应补 Hermes 实际策略下的待审批验收，并用正式打包镜像执行候选发布验收与完整门禁。本轮不宣称完整恢复所有进程内任务：中途 Runtime 重启按失败收敛，不自动重新执行用户任务。

收尾已停止本轮专用控制服务与三个测试 Runtime，保留数据及容器。原有 `4348` 服务仍为 healthy，健康接口返回 HTTP 200。
