# Local 3000 upgrade / 本地服务更新

2026-09-09：将 `mybay-local-control-panel` 更新为本地镜像 `mybay/local:codex-open-20260909`。此前镜像只支持 schema 6，面对现有 schema 7 数据库反复退出；新镜像支持 schema 7，启动后健康检查通过，HTTP 200。

构建修复：`.dockerignore` 原来排除了 Codex bridge，但 Dockerfile 已要求复制该目录。补齐允许规则后完成真实 Docker 构建，镜像内包含 Codex bridge。

更新前停止控制服务，使用 SQLite backup API 备份控制数据库并验证 integrity_check=ok，同时私下备份部署环境及容器配置。没有降级数据库，没有重启 Agent 容器。更新后数据库仍为 schema 7，数据库中全部 38 条实例记录的 ID 集合一致；页面显示当前 8 个 Agent 均运行正常。

浏览器实际进入 `http://localhost:3000/app/deploy`，Codex 选项可见且可选，选择后显示账号文件导入框与“部署 Codex”按钮。本轮验证创建入口，没有代用户创建额外 Agent。

主目录新增本地 `docker-compose.override.yml`，固定镜像及 `.codex-worktrees/runtime-expansion` 构建目录，避免普通本地重建再次使用旧源码。该机器专用覆盖文件不纳入开源发布提交。数据备份保留在主目录 `backups/codex-open-20260909-1788952848948`，含敏感部署配置，不得发布。

配套 JSON 记录镜像、容器、健康状态和数据核验结果。没有推送或发布 GitHub。历史全量门禁超时记录仍保留，本次 Docker 构建与本地验收不替代完整发布门禁。
